# Point the deployed frontend at the hosted API server.
# Leave unset (or empty) for local development, where Vite proxies /api to the local server.
# Vercel: add this under Settings -> Environment Variables (Production) and redeploy.
VITE_API_URL=https://your-backend.onrender.com

# Only VITE_-prefixed variables are bundled at build time.